[package]
name = "mimi_ffi"
version = "0.1.1"
edition = "2024"

# cdylib: Unreal/Unity/Godot에서 불러올 .dll / .so / .dylib 생성
# staticlib: 정적 링크가 필요한 경우 (선택적으로 추가 가능)
[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
mimi_core = { path = "../mimi_core" }
